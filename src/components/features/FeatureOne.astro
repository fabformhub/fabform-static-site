<section>
  <div class="px-8 py-12 mx-auto md:px-12 lg:px-32 max-w-7xl">
    <div class="max-w-2xl mx-auto lg:text-center">
      <p class="text-4xl font-semibold font-display text-blue-950">
        Forms that don’t feel like forms
      </p>
      <p class="text-slate-600 text-base mt-4 max-w-sm lg:mx-auto">
        FabForm turns static forms into friendly conversations—boosting engagement, completion rates, and user delight with every click.
      </p>
    </div>

    <ul class="grid grid-cols-1 gap-12 lg:text-center mt-12 list-none md:grid-cols-3 items-end" role="list">

      <!-- Conversational UI -->
      <li class="flex flex-col">
        <div class="flex justify-center">
          <div class="w-16 h-16 bg-slate-100 rounded-2xl flex items-center justify-center">
            <!-- Chat bubble icon -->
            <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M8 10h.01M12 10h.01M16 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
        </div>
        <div>
          <p class="text-lg mt-8 font-display text-blue-950 lg:text-xl">
            Conversational by design
          </p>
          <p class="mt-2 text-sm text-slate-500">
            Ask one question at a time to keep users focused. It’s like having a real conversation—minimal friction, maximum response.
          </p>
        </div>
      </li>

      <!-- Smart Logic -->
      <li class="flex flex-col">
        <div class="flex justify-center">
          <div class="w-16 h-16 bg-slate-100 rounded-2xl flex items-center justify-center">
            <!-- Flowchart icon -->
            <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M6 7h12M6 17h12M12 7v10" />
            </svg>
          </div>
        </div>
        <div>
          <p class="text-lg mt-8 font-display text-blue-950 lg:text-xl">
            Built for smart flows
          </p>
          <p class="mt-2 text-sm text-slate-500">
            Show the right follow-up questions with conditional logic. No code, no clutter—just clarity and control.
          </p>
        </div>
      </li>

      <!-- Backend -->
      <li class="flex flex-col">
        <div class="flex justify-center">
          <div class="w-16 h-16 bg-slate-100 rounded-2xl flex items-center justify-center">
            <!-- Database icon -->
            <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <ellipse cx="12" cy="5" rx="9" ry="3" stroke="currentColor" stroke-width="2" fill="none"/>
              <path stroke="currentColor" stroke-width="2" fill="none" d="M3 5v6c0 1.66 4.03 3 9 3s9-1.34 9-3V5M3 11v6c0 1.66 4.03 3 9 3s9-1.34 9-3v-6"/>
            </svg>
          </div>
        </div>
        <div>
          <p class="text-lg mt-8 font-display text-blue-950 lg:text-xl">
            Backend included
          </p>
          <p class="mt-2 text-sm text-slate-500">
            Every form submits to a fast, secure backend. Get structured data instantly—ready for export, integration, or automation.
          </p>
        </div>
      </li>

    </ul>
  </div>
</section>

